{
  title: "Items" 
  description: List of items or groups
  type: array
  #format: tabs
  items:
    {
    type: object
    allOf: [ { "$ref": "itemOrGroup.json" } ]
    oneOf: [ # keep in sync with catalog in main schema file.
      { "$ref": "group.json" }
      { "$ref": "wms.json" }
      { "$ref": "wmsGetCapabilities.json" }
      { "$ref": "ckan.json" }
      { "$ref": "csv.json" }
      { "$ref": "abs-itt.json" }
      { "$ref": "wfs.json" }
    ]
  }
}
