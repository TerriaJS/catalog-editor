{
  title: "ABS ITT" # what does ITT stand for?
  type: object
  allOf: [ 
    { "$ref": "catalogItem.json" }
  ]
  properties: {
    type: { type: "string", enum: [ "abs-itt" ] }
    filter:
    {
      items:
      [
        {
          type: string
        }
      ]
      type: array
    }
  }
}

